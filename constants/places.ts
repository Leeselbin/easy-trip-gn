export type Restaurant = {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
};

export type TouristSpot = {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
};
